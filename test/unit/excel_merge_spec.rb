require 'minitest/spec'
require 'minitest/autorun'

require File.expand_path(File.dirname(__FILE__) + "../../../lib/excel_merge")


describe ExcelMerge do
  it "merges an Excel template and a CSV data file" do
    puts "runs"
  end
end
